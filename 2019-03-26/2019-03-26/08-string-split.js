const names = [
  'Jeff',
  'Jimmy',
  'Danica',
  'Dale, Jr.',
];
names.join(', ');
console.log(names.join(', '));

const newnames = names.join(', ');
console.log(newnames);
newnames.split =('');
console.log(newnames.split(' '));
